[mgr]
show_hidden = true

[opener]
pdff = [
	{ run = 'zathura "$@"', block = false, orphan = true }
]

[open]
rules = [
	{ mime = "application/pdf", use = [ "pdff", "reveal" ] }
]

# [plugin]
# prepend_previewers = [
# 	{ url = "*.pdf", run = "zathura" },
# ]
